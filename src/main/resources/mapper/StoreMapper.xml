<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.uhi.gourmet.store.StoreMapper">

	<insert id="insertStore"
		parameterType="com.uhi.gourmet.store.StoreVO">
		INSERT INTO STORE (
		store_id,
		user_id,
		store_name,
		store_tel,
		store_zip,
		store_addr1,
		store_addr2,
		store_lat,
		store_lon,
		store_category,
		store_desc,
		store_time,
		res_unit,
		store_img,
		store_cnt
		)
		VALUES (
		SEQ_STORE.NEXTVAL,
		#{user_id},
		#{store_name},
		#{store_tel},
		#{store_zip},
		#{store_addr1},
		#{store_addr2},
		#{store_lat},
		#{store_lon},
		#{store_category},
		#{store_desc},
		#{store_time},
		#{res_unit},
		#{store_img},
		0
		)
	</insert>

	<select id="selectPopularStore"
		resultType="com.uhi.gourmet.store.StoreVO">
		SELECT * FROM (
		SELECT * FROM STORE
		ORDER BY store_cnt DESC
		)
		WHERE ROWNUM &lt;= 6
	</select>

	<select id="getListStore"
		resultType="com.uhi.gourmet.store.StoreVO">
		SELECT * FROM (
		SELECT * FROM STORE
		WHERE 1=1
		<if test="category != null and category != ''">
			AND store_category = #{category}
		</if>
		<if test="region != null and region != ''">
			AND store_addr1 LIKE '%' || #{region} || '%'
		</if>
		ORDER BY store_cnt DESC
		)
	</select>

	<select id="getStoreDetail" parameterType="int"
		resultType="com.uhi.gourmet.store.StoreVO">
		SELECT * FROM STORE
		WHERE store_id = #{store_id}
	</select>

	<select id="getMenuList" parameterType="int"
		resultType="com.uhi.gourmet.store.MenuVO">
		SELECT * FROM MENU
		WHERE store_id = #{store_id}
	</select>

	<update id="updateViewCount" parameterType="int">
		UPDATE STORE
		SET
		store_cnt = store_cnt + 1
		WHERE store_id = #{store_id}
	</update>

</mapper>